apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: numbers.internal.platform.org
spec:
  compositeTypeRef:
    apiVersion: internal.platform.org/v1alpha1
    kind: Number
  mode: Pipeline
  pipeline:
    - step: compose-nop-resource
      functionRef:
        name: function-numbers
