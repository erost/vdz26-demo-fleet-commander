apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: numbers.platform.org
spec:
  compositeTypeRef:
    apiVersion: platform.org/v1alpha1
    kind: Number
  mode: Pipeline
  pipeline:
    - step: compose-kubernetes-object
      functionRef:
        name: public-function-numbers
